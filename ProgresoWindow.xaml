<Page x:Class="GestionAprendisaje.ProgresoWindow"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Progreso" Height="700" Width="1300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DataGrid x:Name="ProgresoGrid" Grid.Row="0" AutoGenerateColumns="False" Margin="8,85,8,24">
            <DataGrid.Columns>
                <DataGridTextColumn Header="UsuarioID" Binding="{Binding UsuarioID}" />
                <DataGridTextColumn Header="CursoID" Binding="{Binding CursoID}" />
                <DataGridTextColumn Header="Progreso (%)" Binding="{Binding Progreso}" />
                <DataGridTextColumn Header="Última Actualización" Binding="{Binding UltimaActualizacion}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
